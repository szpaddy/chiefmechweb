<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE beans PUBLIC "-//SPRING//DTD BEAN//EN" "http://www.springframework.org/dtd/spring-beans.dtd">

<beans>
	<!-- 要调用的工作类 -->
	<bean id="bean_access_token" class="com.chiefmech.quartz.AccessTokenUpdateJob"></bean>
	<!-- 定义调用对象和调用对象的方法 -->
	<bean id="job_access_token"
		class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<!-- 调用的类 -->
		<property name="targetObject">
			<ref bean="bean_access_token" />
		</property>
		<!-- 调用类中的方法 -->
		<property name="targetMethod">
			<value>execute</value>
		</property>
	</bean>
	<!-- 定义触发时间  每半小时更新一次access_token -->
	<bean id="cron_access_token" class="org.springframework.scheduling.quartz.CronTriggerBean">
		<property name="jobDetail">
			<ref bean="job_access_token" />
		</property>
		<!-- cron表达式 -->
		<property name="cronExpression">
			<value>* 30 * * * ?</value>
		</property>
	</bean>
	<!-- 总管理类 如果将lazy-init='false'那么容器启动就会执行调度程序 -->
	<bean id="startQuertz" lazy-init="false" autowire="no"
		class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="triggers">
			<list>
				<ref bean="cron_access_token" />
			</list>
		</property>
	</bean>

</beans>